# Validation Error Shape V1

- Archived date: `2026-05-06`
- PMO closeout result: `completed and validated`
- Source sprint: `Validation Error Shape V1`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Extended `BadRequestError` in `backend/src/routes/requestValidation.js` with optional internal `code`, `source`, and `details` fields while preserving the default public message `Invalid request body`.
  - Inlined task Zod `safeParse` failure mapping inside `assertZodSchema`, keeping `BadRequestError` as the single 400 error type and avoiding an extra exported validation helper surface.
  - Kept task create/update schemas behavior-compatible with the Zod pilot, including `.passthrough()` unknown-field compatibility and current required/optional field semantics.
  - Left `backend/src/middleware/errorBoundary.js` unchanged because it already serializes only `{ error: error.message || 'Invalid request body' }` for 4xx errors and does not expose internal fields.
  - Added focused backend contract coverage proving task mutation validation still returns public `{ error: 'Invalid request body' }` and that internal Zod-backed validation errors carry `code`, `source`, and `details`.
  - Applied the follow-up simplification before PMO closeout: removed the exported `createZodBadRequestError` helper because it was only used by `assertZodSchema`.
- Validation summary:
  - `npm --prefix backend test -- test/routes.contract-baseline.test.js`
    - Result: passed.
    - Summary: 10 tests passed, 0 failed.
  - `npm --prefix backend test`
    - Result: passed.
    - Summary: 37 tests passed, 0 failed.
  - `npm run check`
    - Result: passed.
    - Summary: lint passed, frontend tests passed with 21 files / 125 tests, backend tests passed with 37 tests, frontend build passed.
    - Note: Vite reported the existing chunk-size warning during build; this sprint did not touch build configuration or frontend bundling.
- Project-specific review summary:
  Not separately stated.
- Unverified areas:
  - No live browser or manual API-server smoke test was performed.
  - No runtime logging path for the new internal metadata was added or validated; the inlined validator mapping only carries metadata for future internal use.
- Residual risks or escalations:
  - No escalation required.
  - The inlined `assertZodSchema` mapping is enough to support the next small Zod boundary if it can live in `requestValidation.js` and use the same public message contract.
  - If future Zod adoption needs cross-route observability, request-specific source names, or structured server-side logging, PMO should schedule a separate small backend validation/error observability slice rather than expanding this sprint retroactively.
- Documentation-sync outcome: `update required and completed`
- Follow-up routing:
  - PMO should keep `Runtime schema errors keep public responses stable while internal shape improves` as the durable rule.
  - Next runtime-schema planning can choose a concrete backend boundary to migrate to Zod, likely Notes/Projects mutations for product data consistency or Auth payloads for account-boundary safety.
