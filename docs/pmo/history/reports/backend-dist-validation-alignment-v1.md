# Backend Dist Validation Alignment V1

- Archived date: `2026-05-06`
- PMO closeout result: `completed and validated`
- Source sprint: `Backend Dist Validation Alignment V1`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Changed backend tests to import backend runtime modules from `backend/dist` instead of `backend/src`.
  - Changed backend `test` so it runs `npm run build:backend` before `node --test`.
  - Removed the source runtime smoke script from the default backend dist readiness gate.
  - Removed the obsolete `smoke:backend-source` package script and deleted `backend/scripts/smokeBackendSourceRuntime.js`.
  - Updated PMO runtime/system baselines to record that default backend validation now exercises the dist runtime path.
  - Did not delete schema/Notes facade or generated scaffolding in this slice.
- Validation summary:
  - `npm run check` passed.
    - Included lint, schema island guard, Notes route island guard, backend dist runtime readiness, frontend tests, backend tests, and frontend build.
    - Backend dist readiness now rebuilds dist, runs the dist boundary guard, and smoke-loads dist runtime.
    - Backend tests rebuild dist before running and import runtime modules from `backend/dist`.
- Project-specific review summary:
  - Required for this sprint: `no`
  - Performed: `no`
  - If skipped, why skipping was acceptable:
    - This sprint changed backend validation wiring and PMO baseline docs only.
    - No UI/browser surface or public API behavior changed.
- Unverified areas:
  - No source runtime smoke remains in the default validation path.
  - The legacy source runtime may no longer be a supported execution path during the migration window.
- Residual risks or escalations:
  - Backend local test runs are heavier because they build backend dist before executing tests.
  - Schema/Notes source compatibility scaffolding still exists and is now ready for a separate cleanup slice.
- Documentation-sync outcome: `updated`
- Follow-up routing:
  - No immediate human decision is required.
  - Next PMO move should retire schema/Notes facade and generated scaffolding now that start, dev, runtime smoke, and backend tests all use dist.
