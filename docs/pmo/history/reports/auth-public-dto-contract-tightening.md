# Auth Public DTO Contract Tightening

- Archived date: `2026-05-07`
- PMO closeout result: `completed and validated`
- Source sprint: `Auth Public DTO Contract Tightening`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Tightened `backend/src/domain/dtos/authDtos.ts` public auth DTO types from broad `unknown` fields to explicit auth response value types for user and invite DTOs.
  - Tightened the auth runtime record types used by the public adapters while preserving existing nullable and missing-value behavior.
  - Preserved `toPublicUserDto` field selection and `disabled === true` boolean normalization.
  - Preserved `toPublicInviteDto` field selection and kept raw invite code/secret material out of the public DTO.
  - Added focused backend DTO contract tests for public user fields, disabled normalization, nullable/missing user handling, and public invite fields.
  - Confirmed non-goals stayed out of scope: no product DTO edits, no frontend auth API/client/UI changes, no auth route response shape changes, and no password/session/invite lifecycle behavior changes.
  - Confirmed no auth public response field was added, removed, or intentionally renamed.
  - Confirmed `backend/src/domain/dtos/productDtos.ts` was not edited.
- Validation summary:
  - `npm --prefix backend run build:backend` passed.
  - `node --test backend/test/auth-dtos.contract.test.js` passed: 3 tests.
  - `npm --prefix backend run test` passed: 46 tests.
  - `npm run check` passed, including frontend lint/test/build and backend lint/build/runtime smoke/test.
- Project-specific review summary:
  - Required for this sprint: `no`
  - Performed: `no`
  - If performed, reviewed surfaces or states:
  - If skipped, why skipping was acceptable: This was a backend DTO type-only sprint with no frontend, interaction, rendering, or browser-visible behavior changes.
- Unverified areas:
  - No browser validation or UI review was performed because it was not expected for this backend DTO type-only change.
- Residual risks or escalations:
  - Public DTO types now intentionally model current adapter output value types; future auth response additions should update the DTO contract tests alongside the adapter.
  - `npm run check` completed with the existing Vite large chunk warning, but no validation command failed.
- Documentation-sync outcome: `reviewed, no update needed`
- Follow-up routing:
  - None.
  - Product DTO contract tightening remains parked as a separate future slice per the handoff.
