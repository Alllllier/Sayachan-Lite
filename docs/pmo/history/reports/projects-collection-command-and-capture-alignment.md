# Projects Collection Command And Capture Alignment

- Archived date: `2026-05-10`
- PMO closeout result: `completed and validated`
- Source sprint: `Projects Collection Command And Capture Alignment`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Aligned Projects with the Notes-proven collection-first creation pattern using the existing CardCollection `command` slot.
  - Added a collection-owned visible `+ New` project command beside the `Projects (...)` title while keeping the active/archived segmented control in the right-side `control` slot.
  - Removed the always-visible top-level `.project-form` from the normal Projects browsing flow.
  - Moved new-project creation into a focused capture surface: centered modal on desktop and bottom-sheet-style surface on mobile.
  - Preserved existing create-project fields, validation messages, status selection, create behavior, and toast path through `useProjectsFeature`.
  - Kept validation errors inside the capture surface and kept the surface open on validation failure.
  - Closed/reset the capture surface after successful create or explicit cancel.
  - Preserved scroll position during open/save/cancel coverage in Projects UI review.
  - Updated focused Projects UI review coverage and screenshots for create validation, successful create, cancel, collection command/control placement, mobile capture, and existing project-card regression states.
  - Explicit out-of-scope confirmation: no project-card task capture redesign, task preview/list density work, AI suggestion reveal/list cleanup, Dashboard quick add, Notes follow-up changes, global Chat/module hover rail changes, broad visual refresh, or backend/API behavior changes were implemented.
- Validation summary:
  - `npm --prefix frontend run typecheck` passed.
  - `npm --prefix frontend run lint` passed.
  - `npm --prefix frontend run test -- src/features/projects/useProjectsFeature.test.ts src/features/projects/projects.rules.test.ts` passed: 2 files, 26 tests.
  - `npm --prefix frontend run test:ui-review -- --grep "Projects UI review"` passed: 2 Projects UI review tests covering desktop and mobile.
  - `npm --prefix frontend run build` passed.
- Project-specific review summary:
  - Required for this sprint: `yes`
  - Performed: `yes`
  - Reviewed surfaces or states:
    - Projects desktop active list with collection header title, `+ New` command, and active/archived segmented control.
    - New-project capture validation state with project name and summary errors.
    - New-project successful create flow closing the capture surface through the existing feature path.
    - New-project cancel flow closing without creating a project.
    - Scroll preservation while opening and closing capture from an in-list browsing position.
    - Existing project-card task preview collapsed/expanded states.
    - Existing task preview active/completed filter state.
    - Existing project-card task capture single and batch modes.
    - Existing AI suggestion reveal and save-as-task state.
    - Existing overflow menu actions.
    - Archived Projects view restore/delete-only state.
    - Mobile active Projects view with bottom-sheet-style capture and no visible collision with the global Chat button or bottom nav.
  - If skipped, why skipping was acceptable: `n/a`
- Unverified areas:
  - Full root `npm run check` was not run. Focused Projects validation, frontend lint, frontend typecheck, isolated Projects UI review, and frontend build were completed.
  - Broad UI review was not rerun because the prior sprint already observed unrelated Chat/Dashboard failures in the broad suite; focused Projects evidence is kept separate here.
  - Backend/API behavior was not revalidated because this sprint intentionally did not change backend behavior.
- Residual risks or escalations:
  - Projects and Notes now both carry local capture modal/bottom-sheet styles. If more surfaces adopt this pattern, a future shared capture primitive may be worthwhile, but this sprint intentionally did not introduce one.
  - CardCollection command-slot compatibility was used as-is; no new shell API was added.
  - Only Projects received targeted UI review in this sprint. Other CardCollection consumers were not re-reviewed beyond typecheck/build.
- Documentation-sync outcome: `reviewed, no update needed`
- Follow-up routing:
  - No architecture escalation is needed for this sprint.
  - PMO may decide whether to schedule a later shared capture/modal primitive after Notes and Projects have both proven the pattern.
  - PMO may continue to track broad UI-review Chat/Dashboard failures separately if they remain known open issues.
