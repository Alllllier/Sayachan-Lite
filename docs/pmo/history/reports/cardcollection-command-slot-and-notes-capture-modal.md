# CardCollection Command Slot And Notes Capture Modal

- Archived date: `2026-05-10`
- PMO closeout result: `completed and validated`
- Source sprint: `CardCollection Command Slot And Notes Capture Modal`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Added a conservative `command` slot to `CardCollection`, rendered after the title and before the existing right-side `control` slot. Existing title/control-only callers remain compatible.
  - Converted Notes to a collection-first surface by removing the page-level Notes header, keeping the active/archived segmented control in the collection control area, and moving the new-note entry into the CardCollection command slot.
  - Kept the Notes command slot entry as the final human-preferred visible `+ New` command. Icon-only variants were considered and attempted during review, but the final accepted direction is the original visible command style.
  - Replaced the always-visible new-note form with a focused capture dialog on desktop and bottom-sheet-style capture surface on mobile.
  - Lazy-mounted the new-note CodeMirror editor only while the capture surface is open; existing in-card edit behavior remains unchanged.
  - Split the CodeMirror runtime imports out of the initial Notes page chunk with an async editor loader. Notes now preloads the editor bundle during browser idle time, then mounts the editor only when the user opens new-note capture or edits an existing note. The editor does not auto-focus the body after loading.
  - Fixed the capture/editor double-border polish issue by limiting the outer `.codemirror-editor` border to the empty loading placeholder state and leaving the loaded CodeMirror instance's own border as the single visible editor frame.
  - Preserved the current browsing position on open/save/cancel paths in the Notes UI review coverage.
  - Updated Notes UI review coverage for command placement, create validation, successful create, cancel, active/archived preservation, lazy editor mount count, and mobile capture separation from the global Chat button.
  - Refreshed Notes UI review screenshots generated by the updated Notes review path.
  - Explicit out-of-scope confirmation: no global hover rail redesign, Chat behavior change, Projects creation redesign, Dashboard quick add, AI reveal/list cleanup, Format note, or broad visual refresh was implemented.
- Validation summary:
  - `npm --prefix frontend run typecheck` passed.
  - `npm --prefix frontend run lint` passed.
  - `npm --prefix frontend run test -- src/features/notes/useNotesFeature.test.ts src/features/notes/notes.rules.test.ts` passed: 2 files, 21 tests.
  - `npm --prefix frontend run test:ui-review -- --grep "Notes UI review"` passed: 2 Notes UI review tests covering desktop and mobile.
  - `npm --prefix frontend run build` passed. Vite reported the existing large Notes chunk warning after build.
  - After the human-review icon-command refinements, reran `npm --prefix frontend run lint`, `npm --prefix frontend run typecheck`, and `npm --prefix frontend run test:ui-review -- --grep "Notes UI review"`; all passed.
  - After the primary-token style adjustment, reran `npm --prefix frontend run lint`, `npm --prefix frontend run typecheck`, and `npm --prefix frontend run test:ui-review -- --grep "Notes UI review"`; all passed.
  - After reverting the command to the final human-preferred visible `+ New` style, reran `npm --prefix frontend run lint`, `npm --prefix frontend run typecheck`, and `npm --prefix frontend run test:ui-review -- --grep "Notes UI review"`; all passed.
  - After CodeMirror bundle lazy-loading and idle preload refinement, reran `npm --prefix frontend run typecheck`, `npm --prefix frontend run lint`, `npm --prefix frontend run test:ui-review -- --grep "Notes UI review"`, and `npm --prefix frontend run build`; all passed. Build output changed the Notes page from the prior 521.02 kB / gzip 181.20 kB chunk to `NotesPage-Bioc2YO3.js` at 18.99 kB / gzip 6.65 kB, with CodeMirror-related async chunks emitted separately and no large-chunk warning.
  - After the double-border editor polish fix, reran `npm --prefix frontend run typecheck`, `npm --prefix frontend run test:ui-review -- --grep "Notes UI review"`, and `npm --prefix frontend run build`; all passed. Build output kept the Notes page at 18.99 kB / gzip 6.65 kB with no large-chunk warning.
  - Attempted `npm --prefix frontend run test -- --runInBand frontend/src/features/notes/useNotesFeature.test.ts frontend/src/features/notes/notes.rules.test.ts`; this failed because this Vitest version does not support `--runInBand`.
  - Attempted `npm --prefix frontend run test -- frontend/src/features/notes/useNotesFeature.test.ts frontend/src/features/notes/notes.rules.test.ts`; this failed because the filters were resolved relative to `frontend/`. The corrected focused test command above passed.
  - Attempted broad `npm --prefix frontend run test:ui-review -- tests/ui-review/notes/review.spec.ts`; the repo script still ran the full UI review suite because it already hardcodes `tests/ui-review`. Notes tests passed during that run, but unrelated Chat and Dashboard tests failed:
    - Chat desktop shell failed waiting for `.chat-popup` to become visible after opening chat.
    - Dashboard desktop saved-task review failed waiting for the `Show less` button.
- Project-specific review summary:
  - Required for this sprint: `yes`
  - Performed: `yes`
  - Reviewed surfaces or states:
    - Notes desktop active list with collection header command and active segmented control.
    - Notes new-note capture validation state.
    - Notes successful create flow closing the capture surface.
    - Notes cancel flow leaving the created list unchanged and preserving scroll position.
    - Notes existing edit validation and save path after lazy new-note editor changes.
    - Notes AI draft reveal and save-as-task path for regression coverage.
    - Notes archived view with restore/delete-only actions.
    - Notes mobile active view with bottom-sheet-style capture and no visible collision with the global Chat button.
    - Human-review refinement visual check confirmed the Notes command is back to the visible `+ New` style on desktop and mobile after icon-only variants were considered.
  - If skipped, why skipping was acceptable: `n/a`
- Unverified areas:
  - Full root `npm run check` was not run end to end after the broad UI-review attempt showed unrelated Chat and Dashboard UI-review failures. Focused Notes validation, frontend lint, frontend typecheck, focused Notes tests, isolated Notes UI review, and frontend build were completed.
  - Backend/API behavior was not revalidated because the sprint intentionally did not change backend behavior.
- Residual risks or escalations:
  - The new Notes capture surface uses the existing Notes form state and resets it on cancel. If future draft-preservation behavior is desired for canceled capture, that should be a separate product decision.
  - `CardCollection` now wraps title and command in a leading flex container. Existing callers without a command slot should render the same, but only Notes received targeted UI review in this sprint.
  - The editor bundle now loads asynchronously. Idle preload should hide most first-use latency, but very slow devices or networks may briefly show the editor loading placeholder before the body editor is ready.
  - The broad UI review command remains awkward to target by file because the script hardcodes `tests/ui-review`; grep filtering worked for this sprint.
- Documentation-sync outcome: `reviewed, no update needed`
- Follow-up routing:
  - No architecture escalation is needed for this sprint.
  - PMO may want to track the unrelated Chat and Dashboard UI-review failures separately if they are not already known.
