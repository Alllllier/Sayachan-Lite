# Backend ESM Cutover

- Archived date: `2026-05-07`
- PMO closeout result: `completed and validated`
- Source sprint: `Backend ESM Cutover`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Switched backend runtime package metadata from CommonJS to ESM with `backend/package.json` `"type": "module"`.
  - Switched backend TypeScript emit and resolution to NodeNext in `backend/tsconfig.json`.
  - Converted backend source modules from `export =`, `import = require`, and local `require(...)` patterns to ESM import/export style.
  - Added explicit `.js` relative specifiers for NodeNext-compatible backend source imports.
  - Preserved service patchability for backend tests by exporting mutable default service objects while also exposing named functions where useful.
  - Preserved the `@allier/sayachan-ai-core` package boundary through `backend/src/ai/bridge.ts`; private_core remains CommonJS and outside backend build output.
  - Converted backend tests from CommonJS `require(...)` to ESM imports against compiled `dist` artifacts.
  - Updated ESLint backend classification so backend tests lint as ESM and backend scripts remain CommonJS `.cjs`.
  - Tightened `backend/scripts/checkBackendDistBuild.cjs` so the dist guard now requires backend package type to remain `"module"` after cutover.
  - Added local external module declarations for the CommonJS private AI core package and `@koa/cors`.
- Validation summary:
  - `npm --prefix backend run check:backend-dist-runtime`
  - `npm --prefix backend run test`
  - `npm run check`
- Project-specific review summary:
  - Required: no
  - Performed: no
  - Reason: this was backend runtime/module-system work only; no UI or product behavior changed.
- Unverified areas:
  - No live external AI provider call was performed.
  - No browser/UI review was performed, as expected for this backend runtime cutover.
- Residual risks or escalations:
  - Some route/middleware helper types still use pragmatic `any` boundaries where earlier CommonJS assertions had hidden Koa router generic complexity. This is behavior-preserving but worth tightening in a future type cleanup.
  - private_core remains CommonJS by design. The backend bridge now isolates that interop, but private_core TS/ESM migration is still a separate future thread.
  - Backend service default objects are retained for test patchability; future test strategy could reduce this need.
- Documentation-sync outcome: `reviewed, no additional baseline update needed`
- Follow-up routing:
  - No blocking escalation.
  - Future candidate: backend ESM follow-up cleanup for route/middleware helper typing and any remaining compatibility comments once private_core direction is settled.
