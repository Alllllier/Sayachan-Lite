# Route State Accessors And Middleware Folder Split

- Archived date: `2026-05-07`
- PMO closeout result: `completed and validated`
- Source sprint: `Route State Accessors And Middleware Folder Split`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Added `backend/src/routes/routeState.ts` with shared runtime-asserting accessors for validated request bodies and parsed ObjectIds.
  - Moved pure ObjectId type/conversion helpers into `backend/src/domain/objectIds.ts`, so domain/services no longer import ObjectId types or converters from route middleware.
  - Moved the stable session cookie name into `backend/src/domain/authSession.ts`, so `sessionCookies.ts` no longer imports `authService` for a constant.
  - Replaced route-local `validatedBody<T>()` helpers and practical direct `ctx.state.objectIds` reads in auth, AI, notes, projects, and tasks routes with the shared route state accessors.
  - Split middleware by concept:
    - app-level middleware: `backend/src/middleware/app/auth.ts`, `backend/src/middleware/app/errorBoundary.ts`
    - route-level middleware/helpers: `backend/src/middleware/route/auth.ts`, `backend/src/middleware/route/currentUser.ts`, `backend/src/middleware/route/requestBodyValidation.ts`, `backend/src/middleware/route/objectIdParsing.ts`
    - session cookie utility: `backend/src/middleware/sessionCookies.ts`
  - Updated source imports, backend tests, and `backend/scripts/checkBackendDistBuild.cjs` dist path assertions for the moved middleware files and new route state artifact.
  - Out-of-scope confirmation: no adjacent refactor or broader PMO/process change was included.
  - Behavior boundary confirmation: no route behavior, auth/session semantics, validation schemas, ObjectId parsing semantics, AI payload behavior, product DTO behavior, or private-core bridge behavior was intentionally changed.
- Validation summary:
  - `npm --prefix backend run build:backend` - passed.
  - `node backend\scripts\checkBackendDistBuild.cjs` - passed after updating moved middleware assertions.
  - `node --test backend\test\auth.skeleton.test.js backend\test\account-isolation.test.js backend\test\routes.contract-baseline.test.js backend\test\routes.behavior-lock.test.js backend\test\routes.restore-scoping.test.js` - passed, 40 tests.
  - `npm --prefix backend run test` - passed, 53 tests.
  - `npm run check` - passed, including lint, backend dist runtime check/smoke, frontend tests, backend tests, and frontend build.
- Project-specific review summary:
  - Required for this sprint: `no`
  - Performed: `no`
  - If performed, reviewed surfaces or states: `n/a`
  - If skipped, why skipping was acceptable: backend structure/import/accessor-only sprint with no UI or browser-facing interaction change.
- Unverified areas:
  - No manual browser validation was performed.
  - Existing local dev servers were detected on ports `3001` and `5173`; they appeared to be pre-existing `tsx src/server.ts` and Vite processes, so they were left running.
- Residual risks or escalations:
  - `routeState.ts` runtime assertions convert missing validated body/ObjectId route state into an unexpected route failure. This should only occur if a route is wired without its existing validation/parsing middleware; existing focused and full backend route tests passed.
- Documentation-sync outcome: `reviewed, no update needed`
- Follow-up routing:
  - None for this sprint.
