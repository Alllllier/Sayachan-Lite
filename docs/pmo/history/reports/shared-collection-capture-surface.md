# Shared Collection Capture Surface

- Archived date: `2026-05-10`
- PMO closeout result: `completed and validated`
- Source sprint: `Shared Collection Capture Surface`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Added `CollectionCaptureSurface` under `frontend/src/components/ui/shell/` as a thin collection-capture shell.
  - Exported the shared shell through `frontend/src/components/ui/shell/index.ts`.
  - The shared shell owns only the duplicated presentation and close wiring: Teleport overlay, desktop modal, mobile bottom sheet, dialog semantics, title/header, close button, backdrop close, Escape close, body slot, and actions slot.
  - Migrated Notes create capture to `CollectionCaptureSurface` while keeping Notes-owned state, validation, submit/cancel handlers, form fields, CodeMirror async loading/idle preload behavior, editor refs, and business logic local in `NotesPanel.vue`.
  - Migrated Projects create capture to `CollectionCaptureSurface` while keeping Projects-owned state, validation, submit/cancel handlers, project status selection, form fields, and business logic local in `ProjectsPanel.vue`.
  - Removed duplicated local capture overlay/modal/bottom-sheet/header CSS from Notes and Projects; retained feature-specific command, field, editor, card, and collection header styles locally.
  - Updated Notes and Projects UI review coverage to explicitly verify shared-shell Escape and backdrop close behavior.
  - Replaced the shared capture close affordance text entity with a real SVG close icon while preserving caller-owned accessible close labels.
  - Refreshed focused Notes and Projects UI review screenshots generated by the updated review paths.
  - Explicit out-of-scope confirmation: no feature state, validation, submit handler, CodeMirror loader, project status logic, note editor logic, task/project/note business behavior, broad modal framework, global Chat/module hover behavior, Dashboard quick add, backend/API behavior, or broad visual redesign was extracted or changed.
- Validation summary:
  - `npm --prefix frontend run typecheck` passed.
  - `npm --prefix frontend run lint` passed.
  - `npm --prefix frontend run test -- src/features/notes/useNotesFeature.test.ts src/features/notes/notes.rules.test.ts src/features/projects/useProjectsFeature.test.ts src/features/projects/projects.rules.test.ts` passed: 4 files, 47 tests.
  - `npm --prefix frontend run test:ui-review -- --grep "Projects UI review"` passed: 2 Projects UI review tests.
  - `npm --prefix frontend run test:ui-review -- --grep "Notes UI review"` passed: 2 Notes UI review tests.
  - `npm --prefix frontend run test:ui-review -- --grep "Notes UI review|Projects UI review"` passed after adding Escape/backdrop assertions: 4 UI review tests.
  - `npm --prefix frontend run build` passed.
  - After the SVG close icon polish, reran `npm --prefix frontend run typecheck`, `npm --prefix frontend run lint`, and `npm --prefix frontend run test:ui-review -- --grep "Notes UI review|Projects UI review"`; all passed.
  - One initial parallel UI-review attempt failed to start the Notes runner because the concurrent Projects runner owned the Playwright web server port. Notes was rerun by itself and passed; the combined grep run also passed.
- Project-specific review summary:
  - Required for this sprint: `yes`
  - Performed: `yes`
  - Reviewed surfaces or states:
    - Notes desktop active list and new-note validation capture.
    - Notes successful create, cancel, Escape close, and backdrop close paths.
    - Notes mobile active view with bottom-sheet capture and Chat button behind the shared overlay.
    - Notes CodeMirror create editor lazy-mount count remained covered by UI review.
    - Projects desktop active list and new-project validation capture.
    - Projects successful create, cancel, Escape close, and backdrop close paths.
    - Projects mobile active view with bottom-sheet capture and Chat button behind the shared overlay.
    - Projects status select remained inside create capture.
    - Existing Notes card edit/archive/AI states and Projects task preview/task capture/AI/archive states remained covered by focused UI review flows.
  - If skipped, why skipping was acceptable: `n/a`
- Unverified areas:
  - Full root `npm run check` was not run. Focused Notes/Projects tests, frontend lint, frontend typecheck, focused UI review, and frontend build were completed.
  - Broad UI review was not run as a single suite because prior sprint evidence noted unrelated Chat/Dashboard failures; focused Notes/Projects evidence is kept separate here.
  - Backend/API behavior was not revalidated because this sprint intentionally did not change backend behavior.
- Residual risks or escalations:
  - The shared shell is intentionally narrow and currently proven only by Notes and Projects create capture. Future surfaces should adopt it in separate bounded work.
  - The shell owns close wiring, so future callers must keep any reset/preserve decisions in their local `@close` handler to avoid pulling business logic into the shell.
  - The component is not a full modal framework and does not include focus trapping; this preserves prior behavior but remains a possible future accessibility improvement if PMO scopes it separately.
- Documentation-sync outcome: `reviewed, no update needed`
- Follow-up routing:
  - No architecture escalation is needed for this sprint.
  - PMO may decide whether future surfaces should adopt `CollectionCaptureSurface` in later creation/list consistency work.
