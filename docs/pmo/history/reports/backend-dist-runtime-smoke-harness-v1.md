# Backend Dist Runtime Smoke Harness V1

- Archived date: `2026-05-06`
- PMO closeout result: `completed and validated`
- Source sprint: `Backend Dist Runtime Smoke Harness V1`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Split the previous inline dist runtime smoke out of `backend/scripts/checkBackendDistBuild.js`.
  - Added `backend/scripts/smokeBackendDistRuntime.js` as the backend-local dist runtime smoke harness.
    - It imports the compiled dist route/runtime graph under mocks.
    - It mocks `mongoose.connect` so no real MongoDB is required.
    - It mocks `Koa.prototype.listen` so no long-running server is bound.
    - It requires `dist/routes`, `dist/routes/ai`, and `dist/server`.
  - Added backend-local package script `smoke:backend-dist`.
    - Command: `npm run build:backend && node scripts/smokeBackendDistRuntime.js`
    - This gives future pre-cutover validation a clear standalone backend-local smoke command.
  - Updated `check:backend-build` to keep the full dry-run gate:
    - `npm run build:backend`
    - `node scripts/checkBackendDistBuild.js`
    - `node scripts/smokeBackendDistRuntime.js`
  - Kept `backend/scripts/checkBackendDistBuild.js` focused on artifact and boundary checks.
  - Preserved all explicit non-goals:
    - no runtime cutover
    - no `start`/`dev` switch
    - no root `npm run check` expansion
    - no ESM
    - no private-core build inclusion
    - no runtime TS loader
    - no route/service/model migration
    - no island/facade deletion
    - no API/Zod behavior change
- Validation summary:
  - `npm --prefix backend run smoke:backend-dist` passed.
    - Rebuilt backend dist with `tsc -p tsconfig.json`.
    - Imported compiled dist runtime graph under mocked `mongoose.connect` and mocked `Koa.listen`.
    - Did not require real MongoDB and did not leave a long-running server.
  - `npm --prefix backend run check:backend-build` passed.
    - Rebuilt backend dist.
    - Ran the artifact/boundary guard.
    - Ran the dist runtime smoke harness.
  - `npm run lint:backend` passed.
    - Required because backend scripts were changed.
  - Boundary spot checks performed:
    - Confirmed `backend/package.json` `start` remains `node src/server.js`.
    - Confirmed `backend/package.json` `dev` remains `node src/server.js`.
    - Confirmed root `npm run check` remains `npm run lint && npm run check:schema-island && npm run check:notes-route-island && npm run test && npm run build`.
    - Confirmed `backend/dist/private_core` is not present.
- Project-specific review summary:
  - Required for this sprint: `no`
  - Performed: `no`
  - If performed, reviewed surfaces or states:
  - If skipped, why skipping was acceptable:
    - This sprint changed backend validation infrastructure only.
    - No UI, browser surface, route behavior, API contract, frontend behavior, or runtime startup mode changed.
- Unverified areas:
  - `npm --prefix backend test` was not run.
    - The handoff did not require backend tests for this low-risk validation-infrastructure slice.
    - No route/service/model/runtime behavior changed.
  - `npm run check` was not run.
    - The handoff explicitly forbids expanding root `npm run check` for backend dist validation in this sprint, and root check was left unchanged.
- Residual risks or escalations:
  - The dist runtime smoke is still a pre-cutover harness, not proof that production should run from `dist`.
  - The smoke imports `dist/server.js` under mocks, so it proves import/startup graph viability but not real MongoDB connectivity or live HTTP behavior.
  - Future island retirement or route migration may require updating both the artifact guard and smoke expectations.
  - `backend/tsconfig.json` still relies on the current dry-run boundary policy, including `noResolve: true`.
- Documentation-sync outcome: `reviewed, no update needed`
- Follow-up routing:
  - Human/PMO approval is still required before:
    - switching runtime from `node src/server.js` to `node dist/server.js`
    - changing `start` or `dev`
    - expanding root `npm run check`
    - switching to ESM
    - including `backend/private_core/**` in the backend build
    - introducing `tsx`, `ts-node`, or another runtime TS loader
    - migrating routes/services/models
    - deleting schema or Notes island generated artifacts/facades
    - changing public API or Zod behavior
