# Private Core Package Import Boundary V1

- Archived date: `2026-05-06`
- PMO closeout result: `completed and validated`
- Source sprint: `Private Core Package Import Boundary V1`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Added backend dependency `@allier/sayachan-ai-core` as `file:private_core/sayachan-ai-core`.
  - Updated backend install state and `backend/package-lock.json` so npm resolves the existing submodule package through `backend/node_modules/@allier/sayachan-ai-core`.
  - Updated `backend/src/ai/bridge.js` to import `@allier/sayachan-ai-core` by package name instead of the relative `../../private_core/sayachan-ai-core` source path.
  - Hardened `backend/scripts/checkBackendDistBuild.js` so the backend dist guard now verifies:
    - backend still depends on `@allier/sayachan-ai-core` through the local file package boundary
    - the AI bridge imports `@allier/sayachan-ai-core` by package name
    - the AI bridge no longer imports `private_core/sayachan-ai-core` by relative source path
    - `backend/private_core/**` is still not emitted under `backend/dist`
  - Updated `docs/pmo/state/backend_ts_build_boundary_decision_prep.md` to record the human decision outcome.
- Validation summary:
  - `npm --prefix backend install @allier/sayachan-ai-core@file:private_core/sayachan-ai-core` passed.
  - `npm --prefix backend run check:backend-build` passed.
  - `npm --prefix backend test` passed.
    - 40 backend tests passed.
  - `npm run lint:backend` passed.
  - `npm run check` passed.
    - frontend lint, backend lint, schema island check, Notes route island check, frontend tests, backend tests, and frontend build all passed.
- Project-specific review summary:
  - Required for this sprint: `no`
  - Performed: `no`
  - If skipped, why skipping was acceptable:
    - This sprint changed backend package resolution and the AI bridge import boundary.
    - No UI/browser surface changed.
    - Existing backend route tests and full root check covered behavior risk.
- Unverified areas:
  - No live external AI provider call was performed.
  - The AI route test path still passed using existing fallback behavior, but this sprint did not attempt provider integration testing.
- Residual risks or escalations:
  - The dependency is local-file based, not a full npm workspace/monorepo setup.
  - Future CI/install environments must initialize the `backend/private_core/sayachan-ai-core` submodule before `npm --prefix backend install`.
  - Schema unified-build inclusion still needs a separate implementation slice; this sprint only removed the private-core resolution blocker.
- Documentation-sync outcome: `updated`
- Follow-up routing:
  - No new human architecture decision is required for this slice.
  - Future human gates remain:
    - runtime cutover to `node dist/server.js`
    - ESM conversion
    - root `npm run check` expansion for backend dist validation
    - deletion of schema/Notes generated artifacts or facades
