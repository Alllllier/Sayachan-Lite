# Backend Start Dist Runtime Cutover V1

- Archived date: `2026-05-06`
- PMO closeout result: `completed and validated`
- Source sprint: `Backend Start Dist Runtime Cutover V1`
- Source report: `state/execution_report.md`
- Delivered summary:
  - Switched backend `npm start` to `npm run build:backend && node dist/server.js`.
  - Preserved backend `npm run dev` as `node src/server.js`.
  - Updated `backend/scripts/checkBackendDistBuild.js` so the backend build guard now requires:
    - `start` is build-backed dist runtime
    - `dev` remains source runtime
    - backend package remains CommonJS
    - private core remains package-boundary based
    - backend dist does not emit private core
  - Updated `docs/pmo/baselines/runtime-baseline.md` to record the new runtime truth.
  - Did not delete schema or Notes generated/facade scaffolding.
  - Did not add backend dist checks to root `npm run check`.
- Validation summary:
  - `npm --prefix backend run check:backend-dist-runtime` passed.
  - `npm --prefix backend run check:backend-build` passed.
  - `npm --prefix backend test` passed.
    - 40 backend tests passed.
  - `npm run lint:backend` passed.
  - `npm run check` passed.
- Project-specific review summary:
  - Required for this sprint: `no`
  - Performed: `no`
  - If skipped, why skipping was acceptable:
    - This sprint changed backend startup scripting and guardrails.
    - No UI/browser surface changed.
    - Source and dist runtime smoke plus backend/root checks covered the behavior risk.
- Unverified areas:
  - A long-running live `npm --prefix backend start` server was not left running.
  - No deployment environment was exercised.
- Residual risks or escalations:
  - `dev` remains source-based by design.
  - Schema and Notes generated/facade scaffolding remains active and should be retired in later explicit cleanup slices.
  - Root `npm run check` still does not include `check:backend-dist-runtime`; this remains a separate decision.
- Documentation-sync outcome: `updated`
- Follow-up routing:
  - No further approval is needed for this cutover slice.
  - Future human-gated decisions remain:
    - whether to switch `dev` to build/restart dist workflow
    - whether to add backend dist runtime checks to root `npm run check`
    - when to delete schema/Notes generated/facade scaffolding
